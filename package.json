{
  "name": "wloc",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test test/*.test.mjs worker/test/*.test.mjs worker/test/*.test.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
